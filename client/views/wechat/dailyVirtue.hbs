<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=1242, user-scalable=no"/>
    <title>建寺-日行一善</title>
    <link rel="stylesheet" href="/css/css.css">
    <script src="/js/jquery-1.8.3.min.js"></script>
    <script src="/js/js.js"></script>
</head>
<body>
<div class="banner"><img src="/images/product_banner2.jpg"></div>

<div class="contentt">
    <div class="text1">
        <span class="a1">金额:</span>
        <span class="a3">元</span>
        <input id="input_amount" type="text" class="a2" placeholder="请点击输入数字">
    </div>
    <div class="text1">
        <span class="a1">回向:</span>
        <input type="text" class="a2 a4" placeholder="以此功德,回向六道众生.">
        <textarea id="input_target" class="a5" style="height: 320px; display: none;"></textarea>
    </div>
    <a id="show-confirm" href="#">确定</a>
</div>
{{>scrollarea}}
{{>aboutmanjusri}}
{{>footnav}}
<script>
    var speed=10;
    demo2.innerHTML=demo1.innerHTML;
    function Marquee(){
        if(demo2.offsetTop-demo.scrollTop<=0)
            demo.scrollTop-=demo1.offsetHeight;
        else{
            demo.scrollTop++
        }
    }
    var MyMar=setInterval(Marquee,speed);

    $("#show-confirm").click(function () {
        var amount = $('#input_amount').val();
        var target = $('#input_target').val();
        if (amount == '') {
            $.toptip('请填写金额。');
            return;
        }
        var url = window.location.href;
        var dataToPost = {
            amount: amount,
        }
        if(target) dataToPost.target = target;

        $.post(url, dataToPost)
                .done(function (data) {
                    if(!data){
                        return;
                    }
                    window.location = data;
                });
    });
</script>
</body>
</html>